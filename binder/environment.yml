# a mybinder.org-ready environment for demoing aolney_jupyterlab_blockly_r_extension
# this environment may also be used locally on Linux/MacOS/Windows, e.g.
#
#   conda env update --file binder/environment.yml
#   conda activate aolney-jupyterlab-blockly-r-extension-demo
#
name: aolney-jupyterlab-blockly-r-extension-demo

channels:
  - conda-forge

dependencies:
  # runtime dependencies
  - python >=3.10,<3.11.0a0
  - jupyterlab >=4.0.0,<5
  # labextension build dependencies
  - nodejs >=18,<19
  - pip
  - wheel
  # additional packages for demos
  # - ipywidgets
